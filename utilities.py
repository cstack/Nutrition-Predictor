

def pretty_print_predictions(x_test, t_test, t_out, data_size):
  (x, t, v) = load_data(data_size)

  for i in range(len(x_test))
    print [vocabulary[j] in range(len(x_test[i])) if (x_test[i][j] == 1)]
    
  